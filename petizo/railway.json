{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd petizo && npm install && npm run build && pip3 install --upgrade pip --no-cache-dir && pip3 install -r ocr_system/requirements.txt --no-cache-dir"
  },
  "deploy": {
    "startCommand": "cd petizo && chmod +x start.sh && ./start.sh",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
