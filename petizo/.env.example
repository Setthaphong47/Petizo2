# JWT Secret Key (IMPORTANT: Change this in production!)
JWT_SECRET=your-secret-key-here-change-this-in-production

# Server Port (Railway will set this automatically)
PORT=3000

# Node Environment
NODE_ENV=production

# OpenRouter AI Configuration
OPENROUTER_API_KEY=your-openrouter-api-key-here
OPENROUTER_API_URL=https://openrouter.ai/api/v1/chat/completions
MODEL_NAME=openai/gpt-4o-mini

# Database (SQLite - no configuration needed)
# Database file will be created automatically at ./petizo.db

# File Upload Configuration
# Files will be stored in ./uploads/ directory
# Make sure Railway Volume is mounted at /app/uploads

# Python OCR System
# Make sure Python 3.x is installed on the server
# Required packages: opencv-python, pytesseract, easyocr, pillow, numpy

# Email Configuration (for Password Reset)
EMAIL_USER=your-email@gmail.com
EMAIL_APP_PASSWORD=your-gmail-app-password
FRONTEND_URL=http://localhost:3000
